---
app_name: "dron-ci-test"
username: dapmoed
docker:
  tag: "latest"
  image: "{{ username }}/{{ app_name }}"
traefik:
  api_host: "api.sloin.ru"
  api_prefix: "/{{ app_name }}"
  network: "traefik-shared"